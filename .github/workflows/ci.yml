name: CI

on:
  push:
  pull_request:

jobs:
  api-tests:
    name: API tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: api
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - uses: gradle/actions/setup-gradle@v4

      - name: Run tests
        run: ./gradlew test --no-daemon

  web-tests:
    name: Web tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    env:
      CI: true
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Run unit tests
        run: npm test

      - name: Build production bundle
        run: npm run build

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run Playwright tests
        run: npm run test:e2e

  # Publishes ownkeep-core to Docker Hub only. Hosted GCP deploy lives in ownkeep-saas.
  docker:
    name: Publish image
    needs: [api-tests, web-tests]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    # Do not put secrets.DOCKERHUB_USERNAME into job outputs — Actions blanks
    # any output that contains a secret, which broke saas-rebuild (empty image).
    outputs:
      app_name: ${{ steps.meta.outputs.app_name }}
      tag_now: ${{ steps.meta.outputs.tag_now }}
      version: ${{ steps.meta.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Resolve image names
        id: meta
        run: |
          APP_NAME=$(grep -E '^name:' compose.yaml | awk '{print $2}')
          TAG_NOW=$(date +'%Y-%m-%d-%H-%M-%S')
          VERSION=$(grep -E '^version' api/build.gradle.kts | head -1 | sed -E 's/.*"([^"]+)".*/\1/')
          echo "app_name=${APP_NAME}" >> "$GITHUB_OUTPUT"
          echo "tag_now=${TAG_NOW}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Prepare image tags
        id: docker
        run: |
          IMAGE="${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.meta.outputs.app_name }}"
          echo "tags=${IMAGE}:latest,${IMAGE}:${{ steps.meta.outputs.version }},${IMAGE}:${{ steps.meta.outputs.tag_now }}" >> "$GITHUB_OUTPUT"

      - name: Build and push core image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.docker.outputs.tags }}
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max

  saas-rebuild:
    name: Rebuild ownkeep-saas
    needs: [docker]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Trigger ownkeep-saas rebuild
        env:
          SAAS_DISPATCH_TOKEN: ${{ secrets.SAAS_DISPATCH_TOKEN }}
          # Reconstruct here (secrets are fine in env; not via job outputs).
          CORE_IMAGE: docker.io/${{ secrets.DOCKERHUB_USERNAME }}/${{ needs.docker.outputs.app_name }}:${{ needs.docker.outputs.tag_now }}
          CORE_VERSION: ${{ needs.docker.outputs.version }}
        run: |
          set -euo pipefail
          if [ -z "${SAAS_DISPATCH_TOKEN}" ]; then
            echo "::error::SAAS_DISPATCH_TOKEN secret is not configured; cannot trigger ownkeep-saas rebuild." >&2
            exit 1
          fi
          if [[ ! "${CORE_IMAGE}" =~ ^docker\.io/[^/]+/[^:]+:.+$ ]]; then
            echo "::error::CORE_IMAGE looks invalid: ${CORE_IMAGE}" >&2
            exit 1
          fi
          GH_TOKEN="$SAAS_DISPATCH_TOKEN" gh api --method POST repos/rzarajczyk/ownkeep-saas/dispatches \
            -f event_type='core-image-published' \
            -f "client_payload[core_image]=${CORE_IMAGE}" \
            -f "client_payload[core_ref]=${{ github.sha }}" \
            -f "client_payload[core_version]=${CORE_VERSION}"
